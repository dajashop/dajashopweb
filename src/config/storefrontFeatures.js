// Keep stock management active in the admin, while its customer-facing UI is
// temporarily disabled. Set this to true when availability should be shown
// and used for storefront actions again.
export const storefrontFeatures = Object.freeze({
  customerStockVisibility: false,
});
